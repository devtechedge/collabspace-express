# Optional. Only used if you run the Express + Socket.io server locally.
# Production (Vercel) uses the always-on Supabase backend instead.
# VITE_API_URL=http://localhost:5000

# Optional overrides. Production already ships with a public collab backend.
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
