# VITE_API_URL points to the Express + Socket.io backend.
# Leave blank or set to http://localhost:5000 for local development.
# The Vercel client deploy has no backend — full multiplayer needs this URL.
VITE_API_URL=http://localhost:5000
